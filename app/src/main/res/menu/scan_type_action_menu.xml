<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:icon="@drawable/ic_add_box" android:id="@+id/addstockorcrypto"
        android:title="@string/addstockcrypto" app:showAsAction="ifRoom" >
        <menu>
            <item android:icon="@drawable/ic_library_add_black" android:id="@+id/action_add_stock" android:title="@string/addstock" />
            <item android:icon="@drawable/ic_toll_black" android:id="@+id/action_add_crypto" android:title="@string/addcrypto" />
        </menu>
    </item>
    <item
        android:id="@+id/target_price"
        android:icon="@drawable/ic_notifications_paused"
        android:title="Standard Price Target"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/trailing_stop"
        android:title="Trailing stop loss"
        app:showAsAction="never"/>

</menu>